{
  "network": {
    "servers": [ "172.17.13.203:5043" ],
    "ssl certificate": "/opt/certs/logstash-forwarder.crt",
    "ssl key": "/opt/certs/logstash-forwarder.key",
    "ssl ca": "/opt/certs/logstash-forwarder.crt",
    "timeout": 15
  },
  "files": [
    {
      "paths": [ "/tmp/feeds/*.log" ],
      "fields": { "type": "scrapy" }
    }
  ]
}
